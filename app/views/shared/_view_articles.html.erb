<div class='row'>
	<div class='span8 offset2'>
		<% if @announcement %>
		<ol class='articles'>
			<%= render @announcement, format: :full %>
		</ol>
		<% end %>
		<ol class='articles'>
			<%= render @articles, format: :full %>
		</ol>
		<%= will_paginate @articles %>
	</div>
</div>
