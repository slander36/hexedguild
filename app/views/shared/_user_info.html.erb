<a href="<%= user_path(current_user.character) %>">
	<h1>
		<%= current_user.character %>
	</h1>
</a>
<span>
	<%- link_to "view my profile", current_user %>
</span>
<span>
	<%= pluralize(current_user.articles.count, "Articles") %>
</span>
